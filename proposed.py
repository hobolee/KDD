# Here we are going to write our proposals. 

# new way of aggregation from the neighbourhood of the original instance
def aggregation(orig_neighs,....)
    dist_prot = []
    return dist_prot


# new way of retireval method of neighourhoods. 
def retrieve_neighbors(testx, instance_prototypes, idx_current_nodes....)
    orig_neighs = []
    return orig_neighs